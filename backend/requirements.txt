fastapi==0.110.0
uvicorn[standard]>=0.27.0,<1.0.0
pydantic>=2.6.1
regex
numpy==1.26.4
soundfile
transformers>=4.44.0
accelerate
huggingface_hub
encodec
ffmpeg-python   # <-- đúng gói, không phải 'ffmeg'
torch --extra-index-url https://download.pytorch.org/whl/cpu
sentencepiece
python-multipart

# Whisper chính thức (fork của OpenAI)
git+https://github.com/openai/whisper.git

# Bark (cài mà không phụ thuộc các lib khác)
git+https://github.com/suno-ai/bark.git --no-deps
